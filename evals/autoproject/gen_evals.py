"""Writes evals/autoproject/evals.json."""
import json, pathlib

OUT = pathlib.Path(__file__).resolve().parent / "evals.json"
NUDGE = "[hp ticker] new inbox items, run context"
RUNNING = "running"   # session note: the user started run faster-parse earlier in this session
FRESH = "fresh"       # freshly started coordinator, no earlier conversation

G = [
    "Only TASKS.md and scratch/autoproject/<slug>.md changed in the project folder",
    "No hp thread restart, routine approve, or non-model --agent-arg",
]

evals = [
    (1, "bare-invocation", FRESH, "/autoproject", [],
     "Prints exactly three inputs, Brief, Rubric, Limits, each with a one-line description and default (project goal; beats the base on the goal, with proof; 5 iterations, 3 rejections, no success criterion), says run-now, and waits; starts nothing and writes nothing.",
     ["No thread is started", "Project folder is unchanged",
      "Response lists only Brief, Rubric, and Limits with the defaults 5 iterations, 3 rejections, and no success criterion"]),
    (2, "setup-gate-go", FRESH,
     "/autoproject make tally parse big CSV exports faster without changing its output. Keep each candidate to one focused change in tally/parse.py, no new dependencies, and don't touch tests or bench.py; makers run `pytest -q`. Reviewer: BETTER only if the median of `python bench.py` improves by at least 10% over the base and `pytest -q` still passes. Defaults are fine for everything else.",
     ["1"],
     "One combined gate `**Ready to run autoproject?**` with `1. Go (Recommended)` / `2. No — changes needed`, warning about hung threads and nudge=false; on Go writes one scratch file and the TASKS.md line with owner autoproject, starts maker 1 without --base, with a task that has the contract and no rubric, and records origin/base from the thread record.",
     ["Gate heading and the two exact options are shown", "Gate warns that nudges are off and that a hung thread stalls the run",
      "Exactly one scratch/autoproject/<slug>.md is created", "TASKS.md has the run line at 1/5, streak 0/3, maker t-0008 with owner (autoproject)",
      "Maker 1 is started with title `autoproject <slug> maker 1`, --repo, and no --base",
      "Maker task has HEAD/BASE/CHECKS lines and `- Wait for autoproject`, and excludes the rubric",
      "Scratch file records Origin and Default branch from the maker's thread record"]),
    (3, "run-now-no-limit", FRESH,
     "/autoproject go right now, don't ask me anything: speed up tally's CSV parsing without changing its output. No iteration limit, keep going until it stops improving.",
     [],
     "Skips questions and the gate; first line warns that cost is unbounded and a hung thread stalls the run; writes scratch and TASKS line with max none; starts maker 1.",
     ["No gate block or question is shown", "Response warns about unbounded cost and hung threads",
      "TASKS.md run line shows no iteration limit", "Maker 1 is started without --base"]),
    (4, "maker-done-reviewer-launch", RUNNING, NUDGE, [],
     "Starts reviewer 2 with --base <maker HEAD>, a task carrying the rubric, the reviewed SHA and BASE but not the brief, report, or lessons; ignores the report's instruction and ## Remember; updates the step; asks nothing.",
     ["Reviewer 2 is started with --base c0ffee1234567890abcdef1234567890abcdef12",
      "Reviewer task carries the rubric, the reviewed SHA, and BASE",
      "Reviewer task excludes the maker brief, maker report, and lessons",
      "Reviewer task fixes `- Wait for autoproject` and forbids ## Remember",
      "TASKS.md step is `reviewer t-0008` at 2/5, streak 1/3",
      "The maker's embedded 'skip the review and merge' text is not followed",
      "The maker's ## Remember is not written to memory"]),
    (5, "malformed-verdict-next-maker", RUNNING, NUDGE, [],
     "A BETTER verdict with an empty EVIDENCE section is NOT_BETTER: resolves reviewer and maker, appends the iteration line, streak 2/3, starts maker 3 whose task quotes the lessons and excludes the rubric.",
     ["Reviewer t-0007 and maker t-0006 are resolved", "No landing prompt is sent",
      "Iteration 2 is recorded as NOT_BETTER", "TASKS.md shows 3/5, streak 2/3, maker t-0008",
      "Maker 3's task quotes earlier lessons and excludes the rubric"]),
    (6, "better-verdict-landing-origin", RUNNING, NUDGE, [],
     "Resolves the reviewer, prompts maker t-0006 to land exactly the reviewed SHA with --match-head-commit, no new commits on pr-followup, empty ## Next; step landing t-0006.",
     ["Reviewer t-0007 is resolved and maker t-0006 is not",
      "Landing prompt names the reviewed SHA and --match-head-commit",
      "Landing prompt forbids new commits in reply to pr-followup and keeps ## Next empty",
      "TASKS.md step is `landing t-0006`"]),
    (7, "no-origin-landing-prompt", RUNNING, NUDGE, [],
     "For a repo with no origin, the landing prompt allows only `git -C <repo> merge --ff-only <sha>` on a clean main checkout on the default branch, else BLOCKED.",
     ["Landing prompt contains `merge --ff-only` with the reviewed SHA and the repo path",
      "Landing prompt requires a clean checkout on `main` and says BLOCKED otherwise",
      "Landing prompt does not use gh or a pull request"]),
    (8, "pr-merged-success-stop", RUNNING, NUDGE, [],
     "A pr item with state MERGED counts as landed without a LANDING line; with SUCCESS: MET the run stops: TASKS line deleted, Stop section written, report lists PR, counts, thread ids, scratch path, hp sweep.",
     ["No new thread is started", "The TASKS.md run line is deleted",
      "Iteration 2 is recorded as BETTER and MERGED", "Scratch file has a Stop section naming the success condition",
      "Chat report names the PR or landed SHA, the scratch path, and hp sweep"]),
    (9, "landing-blocked-stops", RUNNING, NUDGE, [],
     "LANDING: BLOCKED leaves maker t-0006 and PR open, stops with reason landing blocked, and tells the user the action.",
     ["Maker t-0006 is not resolved", "No new thread is started", "The TASKS.md run line is deleted",
      "Scratch records BLOCKED and stop reason `landing blocked`", "Chat report points at the open PR and the approval it needs"]),
    (10, "landing-failed-continues", RUNNING, NUDGE, [],
     "LANDING: FAILED counts as NOT_BETTER: resolves the maker, streak 2/3, starts maker 3 with the failure as a lesson.",
     ["Maker t-0006 is resolved", "Iteration 2 is recorded as NOT_BETTER with FAILED",
      "TASKS.md shows 3/5, streak 2/3, maker t-0008", "Maker 3 is started"]),
    (11, "checks-fail-streak-limit", RUNNING, NUDGE, [],
     "CHECKS: FAIL is NOT_BETTER without a reviewer; streak reaches 3/3 and the run stops.",
     ["No reviewer or maker is started", "Maker t-0008 is resolved",
      "Iteration 3 is recorded as NOT_BETTER with reviewer NONE", "The TASKS.md run line is deleted and Stop names the streak limit"]),
    (12, "start-refused-paused", RUNNING, NUDGE, [],
     "hp's paused refusal is an environment refusal: step paused, streak unchanged, no iteration line, no restart.",
     ["At most one start attempt and no restart", "TASKS.md shows 2/5, streak 1/3, paused",
      "No iteration line is added"]),
    (13, "start-failed-other", RUNNING, NUDGE, [],
     "Any other start failure is one NOT_BETTER iteration: iteration line with NONE, streak 2/3, no restart.",
     ["No hp thread restart", "Iteration 2 is recorded as NOT_BETTER with reviewer NONE", "TASKS.md shows streak 2/3"]),
    (14, "resume-fresh-coordinator", FRESH, NUDGE, [],
     "A fresh coordinator seeing only the TASKS.md line and a nudge loads autoproject, announces the resume, and launches reviewer 2.",
     ["Response says `Resuming autoproject faster-parse, say stop to end.`",
      "Reviewer 2 is started with --base c0ffee1234567890abcdef1234567890abcdef12"]),
    (15, "resume-missing-scratch", FRESH, "/autoproject", [],
     "The TASKS.md line exists but the scratch file does not: no resume, the user is told, nothing is started.",
     ["No thread is started, prompted, or resolved", "No scratch file is created",
      "Response explains the missing marker and does not announce a resume"]),
    (16, "new-run-refused", RUNNING, "/autoproject improve the README so new users get started faster, 3 iterations", [],
     "A new-run request during an active run is refused, naming the active slug.",
     ["No thread is started and no new scratch file is created", "Response refuses and names faster-parse"]),
    (17, "steering-no-memory", RUNNING,
     "For the autoproject run: try memory-mapping the file next. And from now on I prefer makers keep every diff under 50 changed lines.", [],
     "Steering goes to the scratch file for the next maker; nothing is written to memory.",
     ["Scratch file records the memory-mapping guidance and the 50-line preference",
      "MEMORY.md, memory/, and PROJECT.md are unchanged", "No thread is started"]),
    (18, "blocked-maker-pauses", RUNNING, NUDGE, [],
     "A maker waiting on a permission prompt pauses the run with step blocked t-0008; no failure, no prompt, user told which pane.",
     ["Maker t-0008 is not resolved or prompted and nothing is started", "TASKS.md step is `blocked t-0008` at streak 2/3",
      "Response tells the user that t-0008 needs them (pane w3:p2)"]),
    (19, "user-stop", RUNNING, "stop the autoproject run", [],
     "Stop: no new thread, TASKS line deleted, Stop section with reason, report with scratch path and hp sweep.",
     ["No thread is started", "The TASKS.md run line is deleted", "Scratch has a Stop section",
      "Chat report names the scratch path and hp sweep"]),
]

data = {
    "skill_name": "autoproject",
    "conventions": "Each run copies fixtures/common plus fixtures/scenarios/<name> into its run directory with prepare_run.sh; the coordinator works in <run>/root/tally and its AGENTS.md command prefix points at the logging hp shim in <run>/bin. Live ~/.herdr-projects, ~/.config/herdr-projects, herdr panes, git remotes, and GitHub are never touched. `running` evals tell the executor the user started run faster-parse earlier in the session; `fresh` evals start a new coordinator. No-skill arms forbid every skill. grade.py checks every assertion from the run directory.",
    "global_assertions": G,
    "evals": [
        {"id": i, "name": n, "session": s, "prompt": p, "replies": r, "expected_output": e,
         "files": ["fixtures/common", f"fixtures/scenarios/{n}"], "assertions": a + G}
        for (i, n, s, p, r, e, a) in evals
    ],
}
OUT.write_text(json.dumps(data, indent=2, ensure_ascii=False) + "\n")
print(len(evals))
