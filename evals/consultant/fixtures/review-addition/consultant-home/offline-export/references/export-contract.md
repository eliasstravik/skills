# Export contract

- CSV headers follow visible column labels.
- Rows preserve the current visible sort order.
- Formula-like cell values are escaped to prevent spreadsheet execution.
- The cached page can be read when the network is unavailable.
- Syncing new records still requires network access.
