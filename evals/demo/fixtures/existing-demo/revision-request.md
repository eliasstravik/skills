# Harbor briefing revision

Revise the existing `harbor-briefing` demo in place.

- Change the maintenance window to `13:15–14:00 UTC`.
- Change the owner to `Imani Frost`.
- Change the button label to `Confirm readiness`.
- After activation, show `Readiness confirmed for the 13:15 UTC window`.
- Keep the demo visibly labeled as simulated.
- Preserve unrelated user-authored content, including the exact field note and
  the custom accent token.
- Do not create another demo directory.
