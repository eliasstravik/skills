# Proposed `git-safety` skill

Teach an agent to inspect changes with `git status`, review diffs before committing, write concise commit messages, create a feature branch for new work, and avoid deleting uncommitted changes.

The proposal contains no organization-specific commands, private repository facts, selected house conventions, authority rules, current local state, taste decisions, or deterministic helper scripts.
