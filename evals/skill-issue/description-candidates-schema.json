{
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates", "recommended_id"],
  "properties": {
    "candidates": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "description", "rationale"],
        "properties": {
          "id": {"type": "string", "enum": ["A", "B", "C"]},
          "description": {"type": "string", "minLength": 1, "maxLength": 1024},
          "rationale": {"type": "string"}
        }
      }
    },
    "recommended_id": {"type": "string", "enum": ["A", "B", "C"]}
  }
}
