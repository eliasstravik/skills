# skill-issue — preserved no-skill failures

Iteration 2 preserves two independent baseline failures under the gitignored run root.

## Eval 4 — policy authoring

The no-skill arm retained the local policy and removed generic advice, but wrote `Purpose`, `Scope`, `Trigger`, `Inputs`, `Roles and authority`, `Procedure`, `Decision rules`, `Exceptions and escalation`, and `Completion criteria`. A5 failed because this is not the required ordered nine-section shell.

## Eval 5 — overflow package

The no-skill arm correctly created a registry reference and working validator, but wrote `Purpose`, `Admission`, `Inputs`, `Preconditions`, `Procedure`, `Output`, `Verification`, `Failure handling`, and `Resources`. A10 failed because overflow did not preserve the required ordered shell.

These failures prove the chosen form contract adds behavior beyond capable default execution. Decision-only evals passed in both arms; the benchmark does not overclaim value for behavior the baseline already supplied.
