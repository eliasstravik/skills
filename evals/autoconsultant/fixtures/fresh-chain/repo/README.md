# Mica Queue

Mica Queue is a fictional internal CLI that prints a JSON summary of queued
service requests.

Operators currently scan the complete output to find requests older than their
team target. The proposed `--aging-summary` mode should make that scan faster
without changing queue state.

Documentation: https://docs.example.com/mica-queue
