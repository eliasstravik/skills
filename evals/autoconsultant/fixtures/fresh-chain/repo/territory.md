# Aging-summary territory

- Output remains JSON.
- Version one is read-only and dependency-free.
- A request has `id`, `team`, `created_at`, and `status`.
- Cancelled and completed requests must never count as aging.
- The exact threshold and grouping are unresolved.
